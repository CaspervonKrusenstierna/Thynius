import React from 'react'
import "./VerificationMessage"

const VerificationMessage = (props) => {
  return (
    <h className='VerificationMessage'>{props.text}</h>
  )
}

export default VerificationMessage